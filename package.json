{
  "name": "to-do-lister",
  "version": "1.0.0",
  "description": "Task management application using JavaScript and JSON Server",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT",
    "dev": "json-server db.json --host localhost --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}